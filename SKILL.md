---
name: AI 图表设计生成器
version: 1.0.0
author: content-matrix
description: 输入图表类型与关键节点，自动输出带配色规范和排版层级的 SVG/HTML 设计稿。
---

# AI 图表设计生成器

## 工作步骤

1. **辨识图表类型**：根据用户描述选择最匹配的模板（架构/流程/时序/状态机/ER/时间线等），不确定时先问一句或给两个候选。
2. **抽取关键节点**：从用户输入里识别实体（节点）、关系（连线）、方向（从左到右 / 从上到下 / 自由拓扑）和层级（一级节点/二级节点）。
3. **套用配色规范**：默认使用浅色或深色通用配色板。如果用户给了品牌色（hex 值或主色描述），按主色 + 互补 + 中性色推导一套 5 色调色板，并校验前景对比度 ≥ 4.5:1。
4. **按模板绘制**：固定每个模板的节点尺寸、字号、内边距和连线箭头。节点矩形默认 8px 圆角，标题字号 ≥ 14px，关键标签字号 ≥ 12px。
5. **可读性检查**：跑一遍简单规则——标签不超出节点矩形、不与连线重叠、相邻节点保留 24px 以上间距，必要的分组用浅色背景块包起来。
6. **输出**：默认输出单文件 HTML（内联 SVG + CSS 变量），方便复制粘贴到 Notion、语雀、腾讯文档、Marvis 等。同步给一份纯 SVG 备用。

## 使用建议

- 用户说"画个流程图" → 反问一句：流程从 A 到 B 分几步、是否需要判断分支、谁触发。
- 用户上传参考图 → 提取风格特征（色温、密度、层级感），而不是复刻内容。
- 输出文件命名建议：`{类型}_{主题}_{日期}.html`。
- 多张图组合时，统一用一份 CSS 变量表，便于后续一并调风格。

## 红线

- 不复刻受版权保护的插画、Logo、品牌字体。
- 不渲染真人或敏感人物肖像。
- 不渲染歧视、暴力、血腥等不当内容。
- 不诱导用户付费订阅或绑定具体商业服务。
- 数字、来源若不确定，必须标"示例"或"待校对"。
